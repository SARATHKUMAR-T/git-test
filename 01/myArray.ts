let superHeros: string[] = [];
let numbers: number[] = [];
let num: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

allUsers.push({
  name: "sarath",
  isActive: true,
});

superHeros.push("venom");
numbers.push(parseInt("3"));
